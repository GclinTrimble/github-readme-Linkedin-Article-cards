<!-- github-readme-Linkedin-Article-cards -->
<svg xmlns="http://www.w3.org/2000/svg" width="{{ width }}" height="{{ height }}"
    viewBox="0 0 {{ width }} {{ height }}" role="img" aria-label="LinkedIn article card">
    {% with clip_id = "card-clip" %}
    {% include "_tile.svg" %}
    {% endwith %}
</svg>
